Complaint:
  summary: Complaint object
  type: object
  properties:
    id:
      type: integer
    productId:
      type: integer
    content:
      $ref: "complaint-content.yml#/ComplaintContent"
    creationDate:
      type: string
      format: date-time
    creationUser:
      type: string
      minLength: 1
#   TODO: Define country type and restrictions correctly.
  required:
    - id
    - productId
    - content
    - creationDate
    - creationUser
#    - country
